#ifndef ANALYTICS_H
#define ANALYTICS_H

#include <cstring>
#include "book.h"
#include <sys/types.h>
# include "linked_list.h"
#include <iostream>
using namespace std;
typedef struct 
{
	char *string;
}s_array;
void analytics(node *);
void novel(const char *);
void play(const char *);

#endif